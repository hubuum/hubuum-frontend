Hubuum Frontend has been installed.

Image:
  {{ include "hubuum-frontend.image" . }}

Backend target:
  {{ .Values.backend.baseUrl }}

Service:
  {{ include "hubuum-frontend.fullname" . }}:{{ .Values.service.port }}

Authentication sessions require an external Valkey/Redis Secret for every
installation:

  --set valkey.existingSecret.name=hubuum-frontend-valkey

The Secret must contain the Valkey/Redis URL under:
  {{ .Values.valkey.existingSecret.urlKey }}

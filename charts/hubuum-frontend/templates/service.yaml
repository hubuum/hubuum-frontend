apiVersion: v1
kind: Service
metadata:
  name: {{ include "hubuum-frontend.fullname" . }}
  labels:
    {{- include "hubuum-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hubuum-frontend.selectorLabels" . | nindent 4 }}

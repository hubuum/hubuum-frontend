# Base URL for the Hubuum backend API.
# Example: http://hubuum-api:8080
BACKEND_BASE_URL=http://localhost:8080

# Optional Valkey/Redis URL for distributed sessions and shared cache.
# Example: redis://valkey:6379/0
VALKEY_URL=

# Session lifetime for opaque backend tokens (in seconds).
SESSION_TTL_SECONDS=28800

# Optional namespace prefix for session keys in Valkey.
SESSION_PREFIX=hubuum:sess:

# Public app title
NEXT_PUBLIC_APP_NAME=Hubuum Console
